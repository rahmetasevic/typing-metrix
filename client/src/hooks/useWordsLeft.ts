import React, { useEffect, useState } from 'react'
import { useTestStore } from '@stores/TestStore'

export const useWordsLeft = () => {

	// const [time, setTime] = useState<number>(0);
	// const [wordsLeft, setWordsLeft, isStarted, setIsStarted, currWord, testContent] = useTestStore(state => [state.wordsLeft, state.setWordsLeft, state.isStarted, state.setIsStarted, state.currWord, state.testContent]);
	// const [wordsCount, setWordsCount] = useState<number>(0);

	// useEffect(() => {
	// 	if (currWord.index >= testContent.length) {
	// 		setIsStarted(false);
	// 		// stopTimer();
	// 	}

	// }, [isStarted])

	// function handleWordsLeft(): void {
	// 	if (currWord.index >= testContent.length) {
	// 		setIsStarted(false);
	// 		// stopTimer();
	// 	}
	// }
}
